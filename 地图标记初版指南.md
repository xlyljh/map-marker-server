# 地图标记初版指南

---

> ⚠️ **版本说明：当前为 V1（初版）**
>
> 当前版本使用 **Node.js + JSON 文件** 的模式，适合起步阶段使用、验证业务流程。
> 
> **后续升级方向（V2）：** JS​ON 文件 → **数据库**（SQLite / MySQL），后端增加缓存和事务处理，以支持更大规模的数据和多人并发操作。
>
> **当前能做什么：** 多人共享标记、添加/编辑/删除、历史版本、分类管理
> **当前做不到的：** 用户权限管理、数据自动备份、多人同时保存无冲突、海量数据（10万+）
>
> **具体升级内容见第七章《后续升级方向》。**

---

## 一、系统技术架构

### 整体结构图

```
┌─────────────────────────────────────────────────────┐
│                   用户浏览器（前端）                   │
│                                                     │
│  index.html                                         │
│  ├─ HTML + CSS      → 页面布局、样式                   │
│  ├─ 高德地图 JS API → 地图显示、点击事件、地址解析      │
│  ├─ AJAX (fetch)    → 与后端通信（CRUD）               │
│  └─ 用户操作         → 点击地图添加、编辑、删除、导航    │
│                                                     │
│  核心功能实现：                                        │
│  ├─ 地图初始化：AMap.Map('map', {zoom, center})       │
│  ├─ 获取坐标：map.on('click', e => e.lnglat)          │
│  ├─ 地址解析：AMap.Geocoder → getAddress(坐标) → 地址  │
│  ├─ 地址搜索：AMap.AutoComplete / AMap.PlaceSearch    │
│  ├─ 标记渲染：new AMap.Marker({position, title})      │
│  └─ 高德导航：uri.amap.com/marker?position=...        │
└──────────────────┬──────────────────────────────────┘
                   │ HTTP POST/GET/DELETE
                   │ (JSON 格式)
                   ▼
┌─────────────────────────────────────────────────────┐
│                 Node.js 后端服务器                    │
│                                                     │
│  server.js                                          │
│  ├─ Express 框架    → 处理 HTTP 请求（路由）           │
│  ├─ GET  /api/markers       → 获取所有标记           │
│  ├─ POST /api/markers       → 保存所有标记（批量覆盖）  │
│  ├─ POST /api/markers/add   → 添加单个标记            │
│  ├─ PUT  /api/markers/:id   → 更新单个标记            │
│  ├─ DELETE /api/markers/:id → 删除单个标记            │
│  └─ 静态文件托管    → index.html 直接通过服务器访问     │
│                                                     │
│  数据读写：                                           │
│  ├─ fs.readFileSync  → 读取 markers.json             │
│  ├─ fs.writeFileSync → 写入 markers.json             │
│  └─ express.json()   → 解析前端发来的 JSON 数据       │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│                  数据存储层                            │
│                                                     │
│  markers.json                                       │
│  ├─ 位置: /map-server/markers.json                   │
│  ├─ 格式: JSON 数组                                   │
│  ├─ 编码: UTF-8                                      │
│  └─ 内容示例:                                        │
│     [                                              │
│       {                                            │
│         "id": "1234567890",       ← 唯一ID          │
│         "name": "客户张总",        ← 标记名称         │
│         "address": "北京市...",   ← 地址             │
│         "category": "餐饮",       ← 分类             │
│         "remark": "意向客户",     ← 备注             │
│         "lng": 116.397428,       ← 经度（高德坐标）   │
│         "lat": 39.90923,         ← 纬度（高德坐标）   │
│         "createTime": "2026-...",← 创建时间          │
│         "updateTime": "2026-...",← 更新时间          │
│         "history": [...]         ← 修改历史记录      │
│       }                                            │
│     ]                                              │
└─────────────────────────────────────────────────────┘
```

### 各层说明

| 层次 | 技术 | 作用 |
|------|------|------|
| **前端** | HTML + CSS + JavaScript + 高德地图 JS API v2.0 | 界面展示、地图交互、收发数据 |
| **后端** | Node.js + Express | 提供 API 接口、处理数据读写、托管前端页面 |
| **数据层** | JSON 文件 (markers.json) | 永久保存所有标记数据，服务器重启不丢失 |
| **高德 API** | Geocoder / AutoComplete / PlaceSearch | 地址解析（坐标→地址）、地址搜索（关键字→坐标） |

### 一次完整操作的数据流（以"添加标记"为例）

```
用户点击地图位置
    → AMap 捕获 click 事件，返回坐标 (lng, lat)
    → 前端调用高德 Geocoder API：getAddress(lng, lat)
        → 返回中文地址 "北京市朝阳区..."
    → 弹窗让用户输入名称，选择分类
    → 用户点击"保存"
        → 前端构造 JSON 数据
        → fetch POST /api/markers     （批量覆盖保存）
        → 后端 Express 收到请求
        → 后端解析 JSON，写入 markers.json
        → 返回 { success: true }
    → 前端提示"保存成功"，重新渲染标记列表
    → 其他人刷新页面
        → fetch GET /api/markers
        → 后端读取 markers.json
        → 返回所有标记数据
        → 前端渲染到地图和列表上
```

---

## 二、高德地图 API 配置

本工具使用**高德地图 JS API** 作为底层地图服务。代码中已内置了一个可用的 API Key，但如果客户要自己部署使用，**建议换成自己的 Key**，避免 Key 被限流或失效。

### 在哪里修改

打开 `index.html`，找到以下两行：

```html
<!-- 第 166 行：安全密钥 -->
<script type="text/javascript">
    window._AMapSecurityConfig = { securityJsCode: '你的安全密钥' };
</script>

<!-- 第 168 行：API Key -->
<script type="text/javascript" src="https://webapi.amap.com/maps?v=2.0&key=你的API_KEY&plugin=AMap.Geocoder,AMap.AutoComplete,AMap.PlaceSearch"></script>
```

把 `key=` 后面的值换成你自己的 API Key，把 `securityJsCode` 换成对应的安全密钥。

### 如何申请自己的 API Key

1. 打开 **https://lbs.amap.com/**
2. 注册/登录高德开放平台
3. 进入 **控制台 → 应用管理 → 我的应用**
4. 创建新应用 → 添加 **JS API** 服务
5. 复制生成的 **Key** 和 **安全密钥（securityJsCode）**
6. 替换 `index.html` 中对应的值

> ⚠️ 高德地图 JS API 免费额度：每个 Key 每日 100万次请求，对当前场景完全够用。

---

## 三、如何运行（本地测试用）

### 前提条件
- 一台安装了 **Node.js** 的电脑（Windows / Mac 都可以）
- 下载地址：https://nodejs.org （下载 LTS 版本）

### 运行步骤

```
1. 下载代码
   https://github.com/xlyljh/map-marker-server
   点 Code → Download ZIP → 解压

2. 安装依赖（第一次运行）
   cd map-server
   npm install

3. 启动服务器
   node server.js
   
   看到以下信息说明启动成功：
   ============================================================
   🗺️  地图标记服务已启动！
   📡 本地访问: http://localhost:3000
   🌐 局域网访问: http://192.168.x.x:3000
   ============================================================

4. 打开浏览器访问 http://localhost:3000
```

---

## 四、多人共享详解（如何测试）

### 场景一：同办公室测试

几个人在同一个办公室（连同一个 Wi-Fi）：
1. 某台电脑启动 `node server.js`
2. 其他人打开浏览器，输入**那台电脑的 IP:3000**
   （例如 http://192.168.3.6:3000）
3. 所有人打开这个地址，看到的是**同一份数据**
4. A 添加一个标记 → B 点"刷新数据"按钮 → **B 就能看到 A 加的标记**
5. B 也加一个标记 → A 刷新 → **A 也能看到 B 加的标记**

> ✅ 这个场景可以验证"多人共享"功能是否正常

### 场景二：销售在外地（真实使用场景）

需要把服务器部署到 **云服务器**（阿里云、腾讯云等），所有人都通过互联网访问同一个网址。

**效果：**
- 北京销售在客户现场加一个标记 → 上海销售打开就能看到
- 广州销售修改了标记信息 → 全国销售刷新就看到更新
- 所有人的标记都显示在同一张地图上，互不干扰

### 多人共享的核心要点

| 问题 | 答案 |
|------|------|
| 不同地方的人看到的是同一张地图吗？ | **是的，完全一样的地图、同样的标记** |
| A加的标记B能看到吗？ | **能，B刷新页面就能看到** |
| 需要每个人都安装什么吗？ | **什么都不用装，浏览器打开网址就行** |
| 手机能访问吗？ | **能（手机浏览器直接打开）** |
| 数据安全吗？ | **数据在自家服务器上，不外泄** |
| 支持多少人同时用？ | **几百人没问题** |

---

## 五、部署到云服务器（正式上线）

### 为什么需要云服务器？

本地测试时，只有同 Wi-Fi 的人能访问。销售团队分布在全国各地，必须把服务部署到互联网上。

### 推荐方案

| 方案 | 费用 | 说明 |
|------|------|------|
| Zeabur (国内平台) | 免费起步 | 从 GitHub 导入代码，一键部署 |
| 阿里云轻量服务器 | 约 50元/月 | 性能稳定，国内访问快 |
| 腾讯云轻量服务器 | 约 50元/月 | 同上 |

部署完成后，所有销售打开 **http://你的域名或IP:3000** 即可使用。

---

## 六、数据风险与容量说明

### 数据存在哪里？

所有标记数据保存在运行服务器的电脑或云服务器的一个文件中：

```
项目目录/markers.json
```

格式为 JSON 数组，每条标记包含：名称、地址、坐标、分类、备注、历史版本。

### 本地运行的风险点（客户自己电脑跑）

| 风险 | 说明 | 建议 |
|------|------|------|
| **电脑不能关机** | 服务器关了，所有人都访问不了 | 保持开机、不进入睡眠模式 |
| **硬盘故障** | 硬盘坏了，数据全丢 | 定期备份 markers.json |
| **误删文件** | 不小心删了 markers.json | 备份到其他地方 |
| **网络断连** | 电脑断网，其他人连不上 | 使用稳定的网络 |
| **断电** | 突然断电，服务中断 | 建议配 UPS 或有备用方案 |
| **只限内网** | 只有同 Wi-Fi 能访问 | 外网需要内网穿透或云服务器 |

### 数据容量说明

**本地运行（单机模式）：**

| 场景 | 支持标记数 | 说明 |
|------|-----------|------|
| 日常使用 | 几千个 | 流畅运行，毫秒级响应 |
| 轻度使用 | 1万个以内 | 加载和保存正常 |
| 重度使用 | 5万个以内 | 可能感觉轻微延迟 |
| 极限 | 10万+ | 建议升级数据库 |

> 本地模式的数据上限主要取决于 **markers.json 文件大小**，1万个标记约 3~5MB，对于现代电脑完全无压力。

**部署到云服务器后：**

| 配置 | 支持标记数 | 建议场景 |
|------|-----------|----------|
| 最低配云服务器 | 几万个 | 创业团队 |
| 中等配置 | 几十万个 | 中型企业 |
| 高性能 + 数据库 | 数百万+ | 大型企业 |

### 数据备份建议

**最简单的方式：** 直接复制 `markers.json` 文件到别的地方保存。

**推荐频率：**
- 日常使用：**每周备份一次**
- 重要数据：**每天备份一次**
- 云服务器：**设置自动定时备份**

### 多人同时操作时数据会乱吗？

**不会乱。** 每次保存都会完整写入 markers.json 文件，后保存的人会覆盖前一个人的修改。

但在实际使用中很少出现两个人同时按保存的情况。如果担心这个问题，可以升级到数据库版本（V2），数据库能自动处理多人同时写入，互不干扰。

---

## 七、后续升级方向

### V2：引入数据库

当前 V1 使用 JSON 文件存储，适合**起步阶段验证业务流程**。当数据量和用户量增长后，需要升级。

**为什么要升级数据库：**

| 对比项 | V1（JSON 文件） | V2（数据库） |
|--------|----------------|-------------|
| 存储方式 | markers.json 文件 | SQLite / MySQL |
| 支持数据量 | 1万以内流畅 | 几十万~数百万 |
| 多人同时保存 | 后保存的覆盖前面的 | 自动处理，无冲突 |
| 数据备份 | 手动复制文件 | 可设置自动备份 |
| 查询速度 | 加载全部数据 | 按需查询，快 |
| 权限管理 | 无 | 可设用户角色 |
| 需要额外安装 | 无 | 数据库软件 |

**V2 升级内容：**
1. **SQLite（轻量升级）** — 无需安装，自动创建数据库文件，适合几千~几万标记
2. **MySQL / PostgreSQL（专业升级）** — 需要安装数据库服务，适合数万~数百万标记，多人高并发场景

### V3：专业运维

- 用户登录/权限管理
- HTTPS 证书（安全传输）
- 独立域名
- 自动备份机制
- 数据导出（Excel/CSV）
- 操作日志（谁在什么时候改了哪个标记）
- 容器化部署（Docker），便于维护和迁移

---
## 八、常见问题

### Q：怎么给外地的客户/员工演示？
- 临时方案：用内网穿透工具（如 cpolar），生成一个临时公网地址
- 正式方案：部署到云服务器，所有人都能访问

### Q：数据会丢吗？
- 数据在服务器的 `markers.json` 文件中
- 定期备份这个文件即可
- 部署到云服务器后可以设置自动备份

### Q：每个人都要安装 Node.js 吗？
**不需要。** 只有运行服务器的那台电脑需要安装 Node.js，其他人只要用浏览器打开网址就行。

### Q：现在只是初版，后续会收费吗？
**初版不收费。** 后续升级（数据库、权限管理、自动备份等）按工作量评估，具体费用详谈。

---

## 九、联系方式

| 方式 | 信息 |
|------|------|
| 📞 电话 | 13140593660 |
| 💬 微信 | HuiLiJourney_7 |
