# 地图标记工具 - 系统架构与维护指南

---

## 一、当前版本架构（V1：GitHub Pages 部署）

```
┌──────────────────────────────────────────────┐
│             用户浏览器 (所有用户)               │
│                                              │
│  https://xlyljh.github.io/map-marker-server/  │
│                                              │
│  index.html (高德地图JS + 前端逻辑)            │
│         │                                     │
│    ┌────┴────┐                                │
│    │ 读取数据 │  ← 从 raw.githubusercontent.com │
│    │ 保存数据 │  → 通过 GitHub API (需 Token)   │
│    └────┬────┘                                │
│         │                                     │
└─────────┼─────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────┐
│          GitHub 仓库 (云端存储)                │
│                                              │
│  仓库地址:                                    │
│  github.com/xlyljh/map-marker-server         │
│                                              │
│  markers.json  ← 所有标记数据存这里           │
│    ├─ 路径: /markers.json                    │
│    ├─ 格式: JSON 数组                         │
│    ├─ 当前大小: ~1.5KB (5条测试数据)           │
│    └─ 读取地址(公开):                         │
│       raw.githubusercontent.com/xlyljh/      │
│       map-marker-server/master/markers.json  │
│                                              │
│  index.html    ← 前端页面                     │
│  GitHub Pages  ← 自动部署为网页               │
│    └─ 访问地址: xlyljh.github.io/             │
│        map-marker-server/                    │
└──────────────────────────────────────────────┘
```

### 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | HTML + CSS + JavaScript | 纯前端应用，无需后端服务器 |
| 地图 | 高德地图 JS API v2.0 | 地图显示、地址解析、搜索 |
| 存储 | GitHub repo (markers.json) | 数据存储在 GitHub 仓库中 |
| 部署 | GitHub Pages | 静态页面托管，永久免费 |
| 共享 | GitHub API (需 Token) | 通过 API 读写数据，实现多人共享 |

### 通信流程

1. **读取数据** → 从 `raw.githubusercontent.com` GET 请求（无需 Token）
2. **保存数据** → 通过 GitHub Contents API PUT 请求（需要 Token）
3. **网页服务** → GitHub Pages 自动部署 `index.html`

---

## 二、当前版本使用说明（V1）

### 访问地址

**https://xlyljh.github.io/map-marker-server/**

所有人打开这个链接就能看到地图页面（永久在线，24小时可用）。

### 多人数据共享设置

当前版本通过 **粘贴 GitHub Token** 的方式实现多人共享数据：

1. 打开页面
2. 点右上角 **"设置"** 按钮
3. 输入 GitHub 令牌（Token）
4. 点 **"保存令牌"**

**说明：**
- 每一个用户只需要在自己的浏览器粘贴一次，Token 保存在浏览器中，关闭后不丢失
- 所有贴在同一个 Token 的用户，看到的标记数据**完全一致**
- 不贴 Token 也可以打开页面查看标记（只读模式），但不能保存修改

---

## 三、数据存储说明

### 数据存在哪里？

```
仓库: github.com/xlyljh/map-marker-server
文件: markers.json（在仓库根目录）

直接公开访问地址（无需Token）:
https://raw.githubusercontent.com/xlyljh/map-marker-server/master/markers.json
```

所有标记数据以 JSON 格式保存在这个文件中。任何人打开页面时，自动从这个地址读取数据。

### 测试容量评估

| 场景 | 预估上限 | 说明 |
|------|---------|------|
| **日常使用** | 几十~几百个标记 | 目前完全够用 |
| **演示/测试** | 50个以内 | 操作流畅，无感保存 |
| **轻度使用** | 500个以内 | 加载、保存正常 |
| **重度使用** | 1000~2000个 | 文件变大（约50KB~100KB），加载略慢可接受 |
| **超过2000个** | 不建议 | 文件变大、GitHub API 保存变慢 |

**每个标记占用的数据量：** 约 300~500 字节（含名称、地址、备注、历史版本等）

**示例：**
- 5个标记 → 文件约 1.5KB → 加载瞬间完成
- 100个标记 → 文件约 30KB → 加载 < 0.1秒
- 500个标记 → 文件约 150KB → 加载 < 0.5秒
- 1000个标记 → 文件约 300KB → 加载约 1秒

### 多人同时保存时的说明

> **不会丢数据，但后面保存的人会失败，刷新重试即可。**

原因：
- GitHub 每个文件有版本号（SHA）
- 保存时，代码会带上当前看到的文件版本号
- 如果别人在你之前已经保存了，文件版本号变了
- GitHub 会拒绝你的保存请求，提示"保存到云端失败"
- 刷新页面后再试即可

**实际情况：** 很少会遇到同时保存的情况。如果担心，可以升级到服务器版（V2），数据库自动处理并发，不会有任何问题。

---

## 四、当前版本的风险与局限

> ⚠️ 当前版本是快速部署的过渡方案，适合初期验证和演示。

| 风险 | 说明 |
|------|------|
| **Token 管理不便** | 每个用户需要手动粘贴 Token，不够直观 |
| **无权限控制** | 有 Token 就能改所有数据，没有管理员/普通用户之分 |
| **GitHub API 限频** | 免费版 GitHub API 有速率限制（每小时 5000 次），高频保存可能被限 |
| **数据冲突说明** | 多人同时保存时，后保存的人会看到"保存失败"提示，刷新页面再试即可。不会造成数据被覆盖丢失。|
| **数据量限制** | 适合几十到几百个标记，太多会导致 GitHub 文件变大、加载变慢 |
| **功能依赖 GitHub** | 如果 GitHub 被墙或访问不稳定，国内用户可能打不开 |

---

## 五、完整方案架构（V2：服务器部署规划）

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  用户 A 浏览器  │  │  用户 B 浏览器  │  │  用户 C 浏览器  │
│  (任何设备)    │  │  (任何设备)    │  │  (任何设备)    │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       └─────────────────┼─────────────────┘
                         │ HTTPS (外网访问)
                         ▼
               ┌─────────────────┐
               │  云服务器 / VPS   │  ← 阿里云/腾讯云/其他
               │                 │
               │  Node.js 后端    │  ← 处理所有请求
               │  (server.js)    │
               │                 │
               │  Express API    │  ← RESTful 接口
               └────────┬────────┘
                        │
               ┌────────┴────────┐
               │  数据库           │
               │  SQLite → MySQL  │  ← 随数据量升级
               └─────────────────┘
```

### V2 升级后的变化

| 对比项 | V1 (当前) | V2 (完整版) |
|--------|-----------|------------|
| 部署位置 | GitHub Pages | 云服务器 |
| 外网访问 | ✅ 可访问 | ✅ 更快更稳定 |
| 数据共享 | 粘贴 Token | 打开即用，自动同步 |
| 用户权限 | 无 | 可设管理员/普通用户 |
| 数据冲突 | 可能 | 不会，数据库事务保障 |
| 数据量支持 | 几十~几百 | 几千~几万 |
| 备份 | 手动 | 自动定期备份 |

---

## 六、升级路线图

### 第一步：从当前版本到服务器版（推荐优先做）

将后端从 GitHub API 替换为 Node.js 服务器，部署到云服务器。

**预估工作量：** 1-2 天

**完成后效果：**
- 打开网页直接使用，不再需要粘贴 Token
- 多人同步更稳定，不会冲突
- 外网访问速度更快
- 数据自动备份

### 第二步：完善功能

- 用户登录 / 权限管理
- 数据导出（Excel/CSV）
- 地图样式切换
- 批量操作

### 第三步：企业级升级

- 专业数据库（MySQL/PostgreSQL）
- 多地容灾备份
- 独立域名
- 性能监控

---

## 七、成本分析

### V1（当前版本）

| 项目 | 成本 |
|------|------|
| GitHub Pages 托管 | **免费** |
| GitHub API | **免费**（5000次/小时，足够使用） |
| 高德地图 API | **免费** |
| **总计** | **0元/月** |

### V2（服务器版）

| 项目 | 最低配置 | 费用（预估） |
|------|---------|-------------|
| 云服务器（阿里云/腾讯云） | 2核2G | 约 50~100元/月 |
| 域名（可选） | .com/.cn | 约 50元/年 |
| 数据库 | 自建免费 | 0元 |
| **总计** | | **约 50~100元/月** |

### 企业级

| 项目 | 费用 |
|------|------|
| 云服务器升级 | 约 200~500元/月 |
| 专业数据库 | 可选托管，约 50~200元/月 |
| 独立域名 | 约 50~100元/年 |

---

## 八、联系方式

如需升级、功能定制或技术支持，请联系：

| 方式 | 信息 |
|------|------|
| 📞 电话 | 13140593660 |
| 💬 微信 | HuiLiJourney_7 |

*欢迎随时咨询，响应及时。*
