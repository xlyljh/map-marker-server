# 地图标记工具 - 系统架构与维护指南

---

## 一、系统架构总览

```
┌─────────────────────┐     HTTP请求     ┌──────────────────────┐
│   用户浏览器 (前端)    │ ──────────────→ │   Node.js 后端服务器   │
│                      │ ←────────────── │   (Express + REST)   │
│   index.html         │     JSON响应     │   server.js          │
│   (高德地图 + JS)     │                  │                      │
└─────────────────────┘                  └───────┬──────────────┘
                                                 │
                                                 ▼
                                        ┌──────────────────┐
                                        │   markers.json    │
                                        │   (数据存储文件)    │
                                        └──────────────────┘
```

### 各层说明

| 层级 | 技术 | 作用 |
|------|------|------|
| **前端** | HTML + CSS + JavaScript + 高德地图 API | 地图显示、标记操作、用户交互 |
| **后端** | Node.js + Express | 提供 API 接口，处理数据读写 |
| **数据层** | JSON 文件 (markers.json) | 存储所有标记数据 |

### 通信方式

前端通过 HTTP 请求（AJAX/Fetch）与后端通信，数据格式为 JSON。

---

## 二、API 接口说明

| 方法 | 地址 | 说明 |
|------|------|------|
| `GET` | `/api/markers` | 获取所有标记 |
| `POST` | `/api/markers` | 保存所有标记（批量覆盖） |
| `POST` | `/api/markers/add` | 添加单个标记 |
| `PUT` | `/api/markers/:id` | 更新单个标记 |
| `DELETE` | `/api/markers/:id` | 删除单个标记 |

---

## 三、部署与启动

### 前提条件
- 一台安装了 **Node.js** 的电脑（Windows / Mac / Linux 都可以）
- 局域网环境（用于多人访问）

### 启动步骤

#### 第一次运行（安装依赖）

```
cd map-server
npm install
```

#### 日常启动

```
cd map-server
node server.js
```

启动成功后可以看到：

```
============================================================
🗺️  地图标记服务已启动！
📡 本地访问: http://localhost:3000
🌐 局域网访问: http://192.168.x.x:3000
📁 数据文件: D:\...\markers.json
============================================================
```

#### 多人访问

- 启动服务器的电脑作为**服务端**，保持开机+网络连接
- 其他人用手机或电脑，在浏览器输入 **局域网地址**（如 `http://192.168.3.6:3000`）即可使用
- **所有人看到的标记是一样的**，修改后实时同步

#### 开机自启动（Windows）

1. 创建 `start-server.bat` 文件，内容：`cd /d D:\workbuddy\map-server && node server.js`
2. 将此文件放入 Windows 启动文件夹（按 `Win+R` → 输入 `shell:startup`）

---

## 四、当前风险点与注意事项

> ⚠️ 以下为当前版本已知的风险，请注意规避。

### ⚠️ 风险一：服务端电脑不能关机

- 所有用户依赖服务端电脑运行服务器
- 服务端关机或休眠 → 全员无法访问
- **建议：** 服务端保持不关机、不进入睡眠模式

### ⚠️ 风险二：数据丢失风险

- 数据全部保存在唯一的 `markers.json` 文件中
- 文件损坏、被误删、硬盘故障 → 数据丢失
- **建议：** 至少每周手动备份一次 `markers.json`

### ⚠️ 风险三：无用户权限控制

- 任何人都可以添加、编辑、删除所有标记
- 没有管理员/普通用户之分
- **建议：** 小团队内部使用，注意操作规范

### ⚠️ 风险四：数据量限制

- JSON 文件模式适合 **几百到上千条** 标记
- 超过 1000+ 条后加载和保存会明显变慢
- 超过 5000+ 条可能出现卡顿或崩溃

### ⚠️ 风险五：不支持外网访问

- 当前只能在同一个局域网内使用
- 无法在出差、异地等场景下远程访问

### ⚠️ 风险六：服务器意外退出

- Node.js 进程可能因系统更新、蓝屏、电源断电等原因退出
- 退出后需要手动重启
- **建议：** 开机自启动 + 定期检查服务器是否仍在运行

---

## 五、成本控制方案

本工具以 **低成本、易维护** 为设计原则，以下是各阶段成本明细：

### 当前阶段（零成本）

| 项目 | 成本 |
|------|------|
| 高德地图 API | 免费（每日调用量在限额内） |
| 服务器软件 | 免费（Node.js 开源） |
| 服务器硬件 | 现有电脑即可 |

> ✅ **无任何软件授权费、云服务费、API 调用费**

### 阶段二：SQLite 升级（预估成本）

| 项目 | 费用 |
|------|------|
| 升级开发 | **一次性**，可详谈 |
| 服务器 | 仍然用现有电脑，无需新购 |
| 后续维护 | 无额外费用 |

### 阶段三：企业级升级（按需预算）

| 项目 | 费用说明 |
|------|----------|
| 云服务器（阿里云/腾讯云） | 约 500~2000 元/年 |
| 专业数据库 | MySQL / PostgreSQL 自建免费 |
| 升级开发 | 视需求复杂度评估 |

---

## 六、数据量增长后的应对方案

### 当前限制

- 所有标记保存在一个 `markers.json` 文件中
- 适合 **几百到上千个标记** 的场景
- 数据过大时（5000+ 标记）加载和保存会变慢

### 方案一：本地备份 + 定期清理（零成本，推荐初期使用）

```
每周操作：
1. 复制 markers.json → 改名为 markers_2026_05_09.json（带日期）
2. 清理不需要的旧标记
```

### 方案二：切换为 SQLite 数据库（轻量升级）

当标记达到 **1000+** 时，建议升级到 SQLite。这是一个轻量数据库，无需安装额外软件。

**升级方式：** 联系我们，大约 1-2 天完成升级。改造后：

- ✅ 支持 **数万标记** 流畅运行
- ✅ 支持按时间、分类、关键词快速搜索
- ✅ 数据文件体积小、读写快
- ✅ 仍只有一个文件，备份方便

### 方案三：切换为 MySQL / PostgreSQL（企业级）

当需要 **10万+ 标记**、**多级权限管理** 或 **复杂的统计分析** 时，可以使用专业数据库。

**适用场景：**
- 多个项目/地图分开管理
- 需要详细的用户操作日志
- 数据需要定期自动备份
- 多人协作有权限控制需求（谁可以删、谁能改）

---

## 七、日常维护

### 数据备份

数据文件就是 `map-server/markers.json`，只需复制这个文件即可备份全部标记。

建议定期（如每周）备份一次，打包保存到其他位置。

### 数据恢复

1. 停止服务器（按 `Ctrl+C`）
2. 用备份文件覆盖 `markers.json`
3. 重新启动服务器

### 如何关闭服务器

按 `Ctrl+C`，或者直接关闭命令行窗口。

### 如何更新前端样式

编辑 `index.html` 文件中的 CSS 样式部分（`<style>` 标签内），保存后刷新浏览器即可生效。

---

## 八、常见问题

### Q：其他人打不开网页？
- 确认服务器电脑防火墙放行了 3000 端口
- 确认所有人在同一个局域网（连同一个 Wi-Fi）
- 确认地址输入正确（用 `http://` 开头，不是 `https://`）

### Q：数据丢失了？
- 检查 `markers.json` 是否损坏（文件大小异常或乱码）
- 如果备份了，直接恢复
- 否则尝试清空 `markers.json` 内容为 `[]`，重启服务器

### Q：如何在外网访问？
- 当前版本仅支持局域网
- 如需外网访问，需要公网 IP 或内网穿透工具（如 frp、花生壳），升级后可支持

### Q：服务器意外退出了怎么办？
- 重新打开命令行，执行 `node server.js` 启动即可
- 建议设置开机自启动（见第三章）

---

## 九、联系方式

如需升级、功能定制或技术支持，请联系：

| 方式 | 信息 |
|------|------|
| 📞 电话 | 13140593660 |
| 💬 微信 | HuiLiJourney_7 |

*欢迎随时咨询，响应及时。*
