# 地图标记工具 - 系统架构与使用指南

---

> ⚠️ **版本说明：当前为 V1（初版）**
>
> 当前版本使用 **Node.js + JSON 文件** 的模式，适合起步阶段使用、验证业务流程。
> 
> **后续升级方向（V2）：** JS​ON 文件 → **数据库**（SQLite / MySQL），后端增加缓存和事务处理，以支持更大规模的数据和多人并发操作。
>
> **当前能做什么：** 多人共享标记、添加/编辑/删除、历史版本、分类管理
> **当前做不到的：** 用户权限管理、数据自动备份、多人同时保存无冲突、海量数据（10万+）
>
> **具体升级内容见第六章《后续升级方向》。**

---

## 一、系统架构

```
┌──────────┐  ┌──────────┐  ┌──────────┐
│ 销售A     │  │ 销售B     │  │ 销售C     │
│ (手机/电脑)│  │ (手机/电脑)│  │ (手机/电脑)│
└─────┬────┘  └─────┬────┘  └─────┬────┘
      └──────┬──────┴──────┬──────┘
             │            │
             ▼            ▼
    ┌────────────────────────┐
    │     Node.js 服务器      │  ← 运行 server.js
    │                        │
    │   - 提供网页 (index.html)│
    │   - 提供 API 接口       │
    │   - 存储数据 (markers.json)│
    └────────────────────────┘
```

### 核心原理

- **一台电脑运行服务器**（可以是任何人的电脑，也可以是云服务器）
- **所有人访问同一个地址**（服务器地址）
- **所有数据存在服务器的 markers.json 中**
- **任何人添加/编辑/删除 → 其他人刷新就看到**

### 数据流

```
销售A 添加标记 "客户张总"
    → 浏览器发请求到服务器
    → 服务器写入 markers.json
    → 保存成功

销售B 刷新页面
    → 浏览器从服务器读取 markers.json
    → 显示"客户张总"
    → 看到销售A新加的标记
```

---

## 二、如何运行（本地测试用）

### 前提条件
- 一台安装了 **Node.js** 的电脑（Windows / Mac 都可以）
- 下载地址：https://nodejs.org （下载 LTS 版本）

### 运行步骤

```
1. 下载代码
   https://github.com/xlyljh/map-marker-server
   点 Code → Download ZIP → 解压

2. 安装依赖（第一次运行）
   cd map-server
   npm install

3. 启动服务器
   node server.js
   
   看到以下信息说明启动成功：
   ============================================================
   🗺️  地图标记服务已启动！
   📡 本地访问: http://localhost:3000
   🌐 局域网访问: http://192.168.x.x:3000
   ============================================================

4. 打开浏览器访问 http://localhost:3000
```

---

## 三、多人共享详解（如何测试）

### 场景一：同办公室测试

几个人在同一个办公室（连同一个 Wi-Fi）：
1. 某台电脑启动 `node server.js`
2. 其他人打开浏览器，输入**那台电脑的 IP:3000**
   （例如 http://192.168.3.6:3000）
3. 所有人打开这个地址，看到的是**同一份数据**
4. A 添加一个标记 → B 点"刷新数据"按钮 → **B 就能看到 A 加的标记**
5. B 也加一个标记 → A 刷新 → **A 也能看到 B 加的标记**

> ✅ 这个场景可以验证"多人共享"功能是否正常

### 场景二：销售在外地（真实使用场景）

需要把服务器部署到 **云服务器**（阿里云、腾讯云等），所有人都通过互联网访问同一个网址。

**效果：**
- 北京销售在客户现场加一个标记 → 上海销售打开就能看到
- 广州销售修改了标记信息 → 全国销售刷新就看到更新
- 所有人的标记都显示在同一张地图上，互不干扰

### 多人共享的核心要点

| 问题 | 答案 |
|------|------|
| 不同地方的人看到的是同一张地图吗？ | **是的，完全一样的地图、同样的标记** |
| A加的标记B能看到吗？ | **能，B刷新页面就能看到** |
| 需要每个人都安装什么吗？ | **什么都不用装，浏览器打开网址就行** |
| 手机能访问吗？ | **能（手机浏览器直接打开）** |
| 数据安全吗？ | **数据在自家服务器上，不外泄** |
| 支持多少人同时用？ | **几百人没问题** |

---

## 四、部署到云服务器（正式上线）

### 为什么需要云服务器？

本地测试时，只有同 Wi-Fi 的人能访问。销售团队分布在全国各地，必须把服务部署到互联网上。

### 推荐方案

| 方案 | 费用 | 说明 |
|------|------|------|
| Zeabur (国内平台) | 免费起步 | 从 GitHub 导入代码，一键部署 |
| 阿里云轻量服务器 | 约 50元/月 | 性能稳定，国内访问快 |
| 腾讯云轻量服务器 | 约 50元/月 | 同上 |

部署完成后，所有销售打开 **http://你的域名或IP:3000** 即可使用。

---

## 五、数据风险与容量说明

### 数据存在哪里？

所有标记数据保存在运行服务器的电脑或云服务器的一个文件中：

```
项目目录/markers.json
```

格式为 JSON 数组，每条标记包含：名称、地址、坐标、分类、备注、历史版本。

### 本地运行的风险点（客户自己电脑跑）

| 风险 | 说明 | 建议 |
|------|------|------|
| **电脑不能关机** | 服务器关了，所有人都访问不了 | 保持开机、不进入睡眠模式 |
| **硬盘故障** | 硬盘坏了，数据全丢 | 定期备份 markers.json |
| **误删文件** | 不小心删了 markers.json | 备份到其他地方 |
| **网络断连** | 电脑断网，其他人连不上 | 使用稳定的网络 |
| **断电** | 突然断电，服务中断 | 建议配 UPS 或有备用方案 |
| **只限内网** | 只有同 Wi-Fi 能访问 | 外网需要内网穿透或云服务器 |

### 数据容量说明

**本地运行（单机模式）：**

| 场景 | 支持标记数 | 说明 |
|------|-----------|------|
| 日常使用 | 几千个 | 流畅运行，毫秒级响应 |
| 轻度使用 | 1万个以内 | 加载和保存正常 |
| 重度使用 | 5万个以内 | 可能感觉轻微延迟 |
| 极限 | 10万+ | 建议升级数据库 |

> 本地模式的数据上限主要取决于 **markers.json 文件大小**，1万个标记约 3~5MB，对于现代电脑完全无压力。

**部署到云服务器后：**

| 配置 | 支持标记数 | 建议场景 |
|------|-----------|----------|
| 最低配云服务器 | 几万个 | 创业团队 |
| 中等配置 | 几十万个 | 中型企业 |
| 高性能 + 数据库 | 数百万+ | 大型企业 |

### 数据备份建议

**最简单的方式：** 直接复制 `markers.json` 文件到别的地方保存。

**推荐频率：**
- 日常使用：**每周备份一次**
- 重要数据：**每天备份一次**
- 云服务器：**设置自动定时备份**

### 多人同时操作时数据会乱吗？

**不会乱。** 每次保存都会完整写入 markers.json 文件，后保存的人会覆盖前一个人的修改。

但在实际使用中很少出现两个人同时按保存的情况。如果担心这个问题，可以升级到数据库版本（V2），数据库能自动处理多人同时写入，互不干扰。

---

## 六、后续升级方向

### V2：引入数据库

当前 V1 使用 JSON 文件存储，适合**起步阶段验证业务流程**。当数据量和用户量增长后，需要升级。

**为什么要升级数据库：**

| 对比项 | V1（JSON 文件） | V2（数据库） |
|--------|----------------|-------------|
| 存储方式 | markers.json 文件 | SQLite / MySQL |
| 支持数据量 | 1万以内流畅 | 几十万~数百万 |
| 多人同时保存 | 后保存的覆盖前面的 | 自动处理，无冲突 |
| 数据备份 | 手动复制文件 | 可设置自动备份 |
| 查询速度 | 加载全部数据 | 按需查询，快 |
| 权限管理 | 无 | 可设用户角色 |
| 需要额外安装 | 无 | 数据库软件 |

**V2 升级内容：**
1. **SQLite（轻量升级）** — 无需安装，自动创建数据库文件，适合几千~几万标记
2. **MySQL / PostgreSQL（专业升级）** — 需要安装数据库服务，适合数万~数百万标记，多人高并发场景

### V3：专业运维

- 用户登录/权限管理
- HTTPS 证书（安全传输）
- 独立域名
- 自动备份机制
- 数据导出（Excel/CSV）
- 操作日志（谁在什么时候改了哪个标记）
- 容器化部署（Docker），便于维护和迁移

---
## 七、常见问题

### Q：怎么给外地的客户/员工演示？
- 临时方案：用内网穿透工具（如 cpolar），生成一个临时公网地址
- 正式方案：部署到云服务器，所有人都能访问

### Q：数据会丢吗？
- 数据在服务器的 `markers.json` 文件中
- 定期备份这个文件即可
- 部署到云服务器后可以设置自动备份

### Q：每个人都要安装 Node.js 吗？
**不需要。** 只有运行服务器的那台电脑需要安装 Node.js，其他人只要用浏览器打开网址就行。

### Q：现在只是初版，后续会收费吗？
**初版不收费。** 后续升级（数据库、权限管理、自动备份等）按工作量评估，具体费用详谈。

---

## 八、联系方式

| 方式 | 信息 |
|------|------|
| 📞 电话 | 13140593660 |
| 💬 微信 | HuiLiJourney_7 |
