# 地图标记工具 - 系统架构与使用指南

---

## 一、系统架构

```
┌──────────┐  ┌──────────┐  ┌──────────┐
│ 销售A     │  │ 销售B     │  │ 销售C     │
│ (手机/电脑)│  │ (手机/电脑)│  │ (手机/电脑)│
└─────┬────┘  └─────┬────┘  └─────┬────┘
      └──────┬──────┴──────┬──────┘
             │            │
             ▼            ▼
    ┌────────────────────────┐
    │     Node.js 服务器      │  ← 运行 server.js
    │                        │
    │   - 提供网页 (index.html)│
    │   - 提供 API 接口       │
    │   - 存储数据 (markers.json)│
    └────────────────────────┘
```

### 核心原理

- **一台电脑运行服务器**（可以是任何人的电脑，也可以是云服务器）
- **所有人访问同一个地址**（服务器地址）
- **所有数据存在服务器的 markers.json 中**
- **任何人添加/编辑/删除 → 其他人刷新就看到**

### 数据流

```
销售A 添加标记 "客户张总"
    → 浏览器发请求到服务器
    → 服务器写入 markers.json
    → 保存成功

销售B 刷新页面
    → 浏览器从服务器读取 markers.json
    → 显示"客户张总"
    → 看到销售A新加的标记
```

---

## 二、如何运行（本地测试用）

### 前提条件
- 一台安装了 **Node.js** 的电脑（Windows / Mac 都可以）
- 下载地址：https://nodejs.org （下载 LTS 版本）

### 运行步骤

```
1. 下载代码
   https://github.com/xlyljh/map-marker-server
   点 Code → Download ZIP → 解压

2. 安装依赖（第一次运行）
   cd map-server
   npm install

3. 启动服务器
   node server.js
   
   看到以下信息说明启动成功：
   ============================================================
   🗺️  地图标记服务已启动！
   📡 本地访问: http://localhost:3000
   🌐 局域网访问: http://192.168.x.x:3000
   ============================================================

4. 打开浏览器访问 http://localhost:3000
```

---

## 三、多人共享详解（如何测试）

### 场景一：同办公室测试

几个人在同一个办公室（连同一个 Wi-Fi）：
1. 某台电脑启动 `node server.js`
2. 其他人打开浏览器，输入**那台电脑的 IP:3000**
   （例如 http://192.168.3.6:3000）
3. 所有人打开这个地址，看到的是**同一份数据**
4. A 添加一个标记 → B 点"刷新数据"按钮 → **B 就能看到 A 加的标记**
5. B 也加一个标记 → A 刷新 → **A 也能看到 B 加的标记**

> ✅ 这个场景可以验证"多人共享"功能是否正常

### 场景二：销售在外地（真实使用场景）

需要把服务器部署到 **云服务器**（阿里云、腾讯云等），所有人都通过互联网访问同一个网址。

**效果：**
- 北京销售在客户现场加一个标记 → 上海销售打开就能看到
- 广州销售修改了标记信息 → 全国销售刷新就看到更新
- 所有人的标记都显示在同一张地图上，互不干扰

### 多人共享的核心要点

| 问题 | 答案 |
|------|------|
| 不同地方的人看到的是同一张地图吗？ | **是的，完全一样的地图、同样的标记** |
| A加的标记B能看到吗？ | **能，B刷新页面就能看到** |
| 需要每个人都安装什么吗？ | **什么都不用装，浏览器打开网址就行** |
| 手机能访问吗？ | **能（手机浏览器直接打开）** |
| 数据安全吗？ | **数据在自家服务器上，不外泄** |
| 支持多少人同时用？ | **几百人没问题** |

---

## 四、部署到云服务器（正式上线）

### 为什么需要云服务器？

本地测试时，只有同 Wi-Fi 的人能访问。销售团队分布在全国各地，必须把服务部署到互联网上。

### 推荐方案

| 方案 | 费用 | 说明 |
|------|------|------|
| Zeabur (国内平台) | 免费起步 | 从 GitHub 导入代码，一键部署 |
| 阿里云轻量服务器 | 约 50元/月 | 性能稳定，国内访问快 |
| 腾讯云轻量服务器 | 约 50元/月 | 同上 |

部署完成后，所有销售打开 **http://你的域名或IP:3000** 即可使用。

---

## 五、常见问题

### Q：怎么给外地的客户/员工演示？
- 临时方案：用内网穿透工具（如 cpolar），生成一个临时公网地址
- 正式方案：部署到云服务器，所有人都能访问

### Q：数据会丢吗？
- 数据在服务器的 `markers.json` 文件中
- 定期备份这个文件即可
- 部署到云服务器后可以设置自动备份

### Q：每个人都要安装 Node.js 吗？
**不需要。** 只有运行服务器的那台电脑需要安装 Node.js，其他人只要用浏览器打开网址就行。

---

## 六、联系方式

| 方式 | 信息 |
|------|------|
| 📞 电话 | 13140593660 |
| 💬 微信 | HuiLiJourney_7 |
